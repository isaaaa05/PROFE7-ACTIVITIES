# pubspec.yaml
name: activity3
description: A new Flutter project.
publish_to: 'none'

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  animated_text_kit: ^4.2.2  # Added for splash screen animation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true